# 部署说明（待服务器信息确认）

本项目当前只完成本地可运行版本，尚未部署到华为云。服务器实例、公网 IP、安全组、登录方式和凭据均待用户确认，因此本文件不填写虚构链接或密钥。

## 生产入口

生产进程应加载 `wsgi:application`，配置 `FLASK_SECRET_KEY` 和 `PAPER_TRENDS_DATABASE` 环境变量，并把 SQLite 文件放在受保护的数据目录。不要用 `python run.py` 的 Flask 开发服务器对公网提供服务。

## 上线顺序

1. 在服务器创建隔离 Python 环境并安装 `requirements-lock.txt`。
2. 设置环境变量，运行 `flask --app run.py init-db`。
3. 使用已核验的生产 WSGI 服务启动 `wsgi:application`，再由反向代理提供 HTTPS 和静态资源访问。
4. 访问 `/health`，然后按第五轮和第七轮清单检查页面、数据库和错误反馈。
5. 配置备份、日志保留和进程重启策略。

## 外部来源限制

当前项目包含 CVF Open Access 真实来源适配器。导入页按标题扫描 CVPR、ICCV、ECCV 年度目录，再请求对应论文详情页；默认范围为 2020—2026。部署前应确认服务器能够访问 `openaccess.thecvf.com`，并根据服务条款设置合理的请求频率、超时和日志策略。网络失败、页面缺失、标题不一致和字段缺失都必须保留为可解释状态，不能用示例数据代替失败结果。

离线论文管理页的模糊联网查询仍使用数据库中的 `online_papers` 索引，便于无外网环境演示。若要将其替换为其他学术来源，需要单独核对访问许可、字段完整性、分页/限流规则，并补充来源适配器测试；不要把索引样本描述为外部实时结果。
